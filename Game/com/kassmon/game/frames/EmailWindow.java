package com.kassmon.game.frames;

import javax.swing.JButton;
import javax.swing.JInternalFrame;

@SuppressWarnings("serial")
public class EmailWindow extends JInternalFrame {
	
	public EmailWindow () {
		super(null);
		setSize(600, 400);
		setLocation(50, 50);
		setClosable(true);
		
		
		
		JButton previous = new JButton("previous"), next = new JButton("next");
		
		
		
	}
	
}
